apply plugin: 'pmd'

pmd {
    ignoreFailures = false
    ruleSetFiles = files('config/pmd/ruleSets.xml')
}

tasks.withType(Pmd) {
    reports {
        xml.enabled = false
        html.enabled = true
    }
}
